{
  "name": "@example/starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "push": "rsync -rzh --info=progress2 --no-inc-recursive ./dist/ server:/srv/pantherfi/frontend/dist/",
    "buildPush": "yarn build && yarn push"
  },
  "devDependencies": {
    "@astrojs/renderer-vue": "^0.4.0",
    "astro": "^0.23.2",
    "sass": "^1.49.9"
  },
  "dependencies": {
    "highcharts": "^9.3.3"
  }
}
